[package]
name = "houseflow-config"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "1.0.126", features = ["derive"] }
types = { path = "../types/"             , package = "houseflow-types" }

[features]
server = []
device = []
client = []

